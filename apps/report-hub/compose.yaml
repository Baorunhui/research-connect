services:
  report-hub:
    build: .
    restart: unless-stopped
    env_file: ${REPORT_HUB_ENV_FILE:-.env}
    ports:
      - "${REPORT_HUB_PUBLIC_PORT:-58787}:58787"
    volumes:
      - report-hub-data:/data
    environment:
      REPORT_HUB_DATA_DIR: /data

volumes:
  report-hub-data:
